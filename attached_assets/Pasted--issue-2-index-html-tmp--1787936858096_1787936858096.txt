ВЫПОЛНИ все пункты в issue-2/index.html, не спрашивай подтверждения. В /tmp/ три файла: Бріджертони_1.png (дамы в париках), Бріджертони_2.png (пирамида бокалов с бутылкой Frizzante), 1.png (два павлина, горизонтальное).

0. Жёсткое правило: мобильная и планшетная версии утверждены — их рендер меняться НЕ должен. Существующие figures, их base64 и стили <1024px не редактировать. Все изменения — добавление новых элементов + правила в @media (min-width:1024px).

1. Секция Frizzante — desktop-дуэт. Сразу после двух существующих figure (Бріджертони и пирамида) добавь НОВЫЙ блок-дуэт: одна figure, внутри два <img> рядом — base64 из Бріджертони_1.png и Бріджертони_2.png. Подписи продублировать из существующих figure (про бал — под первым, про Frizzante — под вторым). Видимость: новый дуэт — display:none по умолчанию, display:grid/flex в @media ≥1024px; две старые figure — без изменений по умолчанию, display:none !important в @media ≥1024px. Дуэт на десктопе: два фото в ряд ~50/50, одинаковая высота, вертикали показаны с минимальной обрезкой.

2. Зоопарк — desktop-hero павлинов. Тем же приёмом: добавь новый <img> (base64 из 1.png, два павлина, горизонтальный) рядом с существующим hero. Новый — виден только ≥1024px (object-position: center center, прежние desktop-хаки 560px/15% к нему не применять); старый вертикальный hero — display:none !important в ≥1024px, на мобильном без изменений. Карусель НЕ трогать.

3. Картинг — вернуть прежнее вертикальное кадрирование (только desktop-блок). После full-bleed внизу видно много лишних шин. В @media ≥1024px подними нижнюю границу: уменьши высоту контейнера и/или сдвинь object-position к верху — в кадре карты и люди, шины переднего плана срезаны. Ширину full-bleed сохранить.

4. Нумерованные списки — в одну колонку (desktop). Списки «кружок+текст» («Культовий коктейль "Епплтіні"…» в GREENDAY и «П'ять команд-переможців» в Герої полів) рендерятся в две колонки. В @media ≥1024px с !important — одна колонка, порядок 1-2-3-4-5 сверху вниз. Мобильную раскладку не трогать. Секцию «Як стати легендою офісу» не трогать.

5. Проверка регрессии мобилки: после правок — скриншот 390px секций Frizzante и Зоопарк, подтверди пиксельную идентичность прежнему виду.

6. Commit + git commit --amend --reset-author + push. Скриншоты 1280px: дуэт Frizzante, hero павлинов, картинг, оба списка.