ВЫПОЛНИ все пункты ниже в issue-2/index.html, не спрашивай подтверждения. Все правки — ТОЛЬКО desktop, только внутри блока @media (min-width:1024px) в конце <style>, с !important. Мобильные и планшетные стили не трогать.

ШАГ 0 — диагностика (обязательно, до правок). Предыдущие правки (hero Бріджертони 520px, пирамида 560px, павлин 560px, nowrap для «~1 РІК») визуально НЕ применились. Найди причину: открой файл, найди эти правила в desktop-блоке и сверь селекторы с реальными классами/структурой HTML этих элементов. Если селектор не совпадает или перебивается inline-стилем — исправь селектор и добавь !important. В ответе перечисли: какой селектор был, какой стал, почему не работал.

1. «Як стати легендою офісу» — заголовок над текстом. Сейчас жирный заголовок пункта («Кава — твоя суперсила.») стоит колонкой слева от описания. На десктопе размести заголовок НАД текстом описания (вертикально, в одной колонке), кружок с номером остаётся слева от заголовка. Зигзаг текст/мем не трогать.

2. Бріджертони hero (бал, серый парик). Отдалить кадр: контейнер выше (мин. 560px) + object-position: center 38%. Убедись, что правило реально применяется (шаг 0).

3. Фото Сергій Рябцун (Герої полів, правая карточка). Его портрет визуально значительно крупнее левого фото (Беззубенко). Приведи обе фотографии ТП к одинаковой высоте контейнера, для Рябцуна — object-position: center top или уменьшенный масштаб, чтобы кадрирование лица было сопоставимо с левым фото.

4. Картинг. Фото обрезано по бокам — на десктопе сделай его full-bleed на всю ширину контентной колонки секции (шире соседних фото, до краёв контейнера), высоту подбери без искажений.

5. Пирамида бокалов (Setter/Frizzante). Отдалить: контейнер мин. 560px + object-position: center 45%. Проверить применение.

6. «~1 РІК» (stats Лозова/супервайзер). Сейчас «~1» и «РІК» на разных строках. Найди реальную разметку этого элемента: если это два отдельных элемента — объедини в одну строку (display:inline / правка разметки этого stats-элемента), плюс white-space:nowrap. Результат: «~1 рік» одной строкой.

7. Павлин hero (Зоопарк). Отдалить: контейнер мин. 560px + object-position: center 15%. Проверить применение.

8. Финал: commit + git commit --amend --reset-author + push. Скриншоты каждого исправленного места на ширине 1280px. В отчёте — ответ по шагу 0 (почему прошлые правила не применялись).